// null -> poiniting to null obj, Nothing
// Undefined -> declared but not assigned

var a = 0;

alert(typeof(a));
alert(typeof(null));
alert(typeof(Math));
alert(typeof(alert));

alert(`this is ${1+1}`);